#include <stdio.h>

int main() {
    int x = 1;
    int y = 2;
    if ( x == y)
    {
        char z = 'a';
        printf("%c", z);
    }
    else
    {
        int z = 5;
        printf("%d", z);
    }
    return 0;
}