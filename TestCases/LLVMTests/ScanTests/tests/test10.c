#include <stdio.h>

//TODO
int mul(int x){
    return x * 5;
}

int main(){
    int* k;
    int result = mul(3);
    printf("%d", result);
    printf(mul( scanf("%d", &result))     );
    printf("%d", mul(result));
    return 0;
}